[build]
builder = "NIXPACKS"
buildCommand = "cd apps/api && npm install && npx prisma generate && npx prisma migrate deploy"

[deploy]
startCommand = "cd apps/api && npm start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
